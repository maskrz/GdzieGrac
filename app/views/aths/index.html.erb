<h1>Listing aths</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @aths.each do |ath| %>
      <tr>
        <td><%= ath.name %></td>
        <td><%= link_to 'Show', ath %></td>
        <td><%= link_to 'Edit', edit_ath_path(ath) %></td>
        <td><%= link_to 'Destroy', ath, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ath', new_ath_path %>
