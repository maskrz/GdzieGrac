<%= form_for(@ath) do |f| %>
  <% if @ath.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ath.errors.count, "error") %> prohibited this ath from being saved:</h2>

      <ul>
      <% @ath.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
