<%= form_for(@eplayer) do |f| %>
  <% if @eplayer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eplayer.errors.count, "error") %> prohibited this eplayer from being saved:</h2>

      <ul>
      <% @eplayer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
